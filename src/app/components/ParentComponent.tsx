// ParentComponent.tsx
"use client";
import React from "react";
import Header from "./Header";

const ParentComponent = () => {
  return (
    <div>
      {/* Other content */}
      <Header />
    </div>
  );
};

export default ParentComponent;
